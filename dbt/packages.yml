packages: []
# NOTE: Elementary was removed -- see dbt_project.yml for why. No dbt
# packages are currently required for this project. `dbt deps` is still
# safe to run with an empty list; it just installs nothing.
